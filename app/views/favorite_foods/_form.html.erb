<%= form_with(model: favorite_food) do |form| %>
  <% if favorite_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite_food.errors.count, "error") %> prohibited this favorite_food from being saved:</h2>

      <ul>
        <% favorite_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :food, style: "display: block" %>
    <%= form.text_field :food %>
  </div>

  <div>
    <%= form.label :user, style: "display: block" %>
    <%= form.text_field :user %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
